<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <!-- No action bar No statusbar-->
    <style name="Theme.Freeland" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- 应用的主要色调，actionBar默认使用该颜色，Toolbar导航栏的底色 -->
        <item name="colorPrimary">@color/colorPrimaryDark</item>
        <!-- 应用的主要暗色调，statusBarColor默认使用该颜色 -->
        <item name="colorPrimaryDark">@color/theme</item>
        <!-- 应用的强调色，CheckBox，RadioButton，SwitchCompat等一般控件的选中效果默认采用该颜色   Accent 强调-->
        <item name="colorAccent">@color/colorPrimaryDark</item>
        <!-- 窗口背景颜色 -->
        <item name="android:windowBackground">@color/bg_public</item>
        <!-- 窗口无默认标题栏 -->
        <item name="android:windowNoTitle">true</item>
    </style>
    <!-- Base application theme. end -->
</resources>