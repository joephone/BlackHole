<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.transcendence.gallery">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.Freeland"
        android:supportsRtl="true">

        <activity android:name=".activity.GalleryMainActivity" />

        <activity android:name=".viewpager.VideoPlayActivity" />

        <activity android:name=".activity.FirstActivity" />

        <activity android:name=".activity.SecondActivity" />

        <activity android:name=".activity.ThirdActivity" />

        <activity android:name=".activity.FourActivity" />

        <activity android:name=".activity.FiveActivity" />

        <activity
            android:name="com.transcendence.picture.luban.LubanActivity" />



    </application>

</manifest>