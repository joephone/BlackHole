<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.transcendence.dongnao">

    <application
        android:name=".base.app.DouyaApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_douya"
        android:label="@string/app_douya_name"
        android:roundIcon="@mipmap/ic_douya_round"
        android:supportsRtl="true"
        android:theme="@style/NoTitleAppTheme">
        <activity
            android:name=".zee.lsn13.act.MaterialDesignActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".base.app.DouyaMainActivity" />
        <!--<activity android:name=".zee.lsn14.act.MaterialDesignActivity" />-->
    </application>

</manifest>